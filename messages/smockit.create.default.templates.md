# summary

Generate all predefined data template files

---

# description

Creates all available template files (default, salesprocess, taskray, etc.) in the data_gen/templates directory, preparing them for user customization and data generation.

---

# examples

- `sf smockit create default templates`